import React from 'react';

const GPIO = () => {
    return (
        <div>
            <h3>Portas de Entrada e Saída (GPIO)</h3>
            <p>
            Portas de Entrada e Saída (GPIO)
            </p>
        </div>
    );
};

export default GPIO;

